{% if page.url == "/" %}
  <section class="[ mambo-hero mambo-section ]" id="{{ hero.heroHeader | slug }}">
    <div class="[ container container-default ][ flow ]">
      {% if hero.heroHeader %}
        <span id="skip-to-content">
          <h1>{{ hero.heroHeader | safe }}</h1>
        </span>
      {% endif %}
      {% if hero.heroBody %}
        <div class="[ wysiwyg ]">{{ hero.heroBody | md | safe }}</div>
      {% endif %}
      {% if hero.showHeroButtonLeft == true %}
        <a class="[ mambo-anchor proposal ]" href="{{ hero.heroButtonLeftURL | safe }}">
          {{ hero.heroButtonLeftLabel | safe }}
          <span>
            {% include 'static/images/hero/button-container.njk' %}
          </span>
        </a>
      {% endif %}
      {% if hero.showHeroButtonRight == true %}
        <a href="{{ hero.heroButtonRightURL | safe }}">{{ hero.heroButtonRightLabel | safe  }}</a>
      {% endif %}
    </div>
  </section>
  {% endif%}

  {% if page.url == "/events/" %}
    <section class="[ mambo-hero mambo-section ]" id="{{ hero.heroHeader | slug }}">
      <div class="[ container container-default ][ flow ]">
        {% if hero.heroHeader %}
          <span id="skip-to-content">
            <h1>{{ hero.heroHeader | safe }}</h1>
          </span>
        {% endif %}
        {% if hero.heroBody %}
          <div class="[ wysiwyg ]">{{ hero.heroBody | md | safe }}</div>
        {% endif %}
        {% if hero.showHeroButtonLeft == true %}
          <a class="[ mambo-anchor proposal ]" href="{{ hero.heroButtonLeftURL | safe }}">
            {{ hero.heroButtonLeftLabel | safe }}
            <span>
              {% include 'static/images/hero/button-container.njk' %}
            </span>
          </a>
        {% endif %}
        {% if hero.showHeroButtonRight == true %}
          <a href="{{ hero.heroButtonRightURL | safe }}">{{ hero.heroButtonRightLabel | safe  }}</a>
        {% endif %}
      </div>
      <div class="events-decorative-svg">
        {% include "static/images/hero/events-cluster.svg" %}
      </div>
    </section>
  {% endif %}

  {% if page.url == "/about/" %}
    <section class="[ mambo-hero mambo-section ]" id="{{ hero.heroHeader | slug }}">
      <div class="[ container container-default ][ flow ]">
        {% if hero.heroHeader %}
          <span id="skip-to-content">
            <h1>{{ hero.heroHeader | safe }}</h1>
          </span>
        {% endif %}
        {% if hero.heroBody %}
          <div class="[ wysiwyg ]">{{ hero.heroBody | md | safe }}</div>
        {% endif %}
        {% if hero.showHeroButtonLeft == true %}
          <a class="[ mambo-anchor proposal ]" href="{{ hero.heroButtonLeftURL | safe }}">
            {{ hero.heroButtonLeftLabel | safe }}
            <span>
              {% include 'static/images/hero/button-container.njk' %}
            </span>
          </a>
        {% endif %}
        {% if hero.showHeroButtonRight == true %}
          <a href="{{ hero.heroButtonRightURL | safe }}">{{ hero.heroButtonRightLabel | safe  }}</a>
        {% endif %}
      </div>
    </section>
    {% endif%}

    {% if page.url == "/committee/" or page.url == "/committee/executive/" or page.url == "/committee/programming/" or page.url == "/committee/design/" or page.url == "/committee/communications/" or page.url == "/committee/operations/" %}
      <section class="[ mambo-hero mambo-section ]" id="{{ hero.heroHeader | slug }}">
        <div class="[ container container-default ][ flow ]">
          {% if hero.heroHeader %}
            <span id="skip-to-content">
              <h1>{{ hero.heroHeader | safe }}</h1>
            </span>
          {% endif %}
          {% if hero.heroBody %}
            <div class="[ wysiwyg ]">{{ hero.heroBody | md | safe }}</div>
          {% endif %}
          {% if hero.showHeroButtonLeft == true %}
            <a class="[ mambo-anchor proposal ]" href="{{ hero.heroButtonLeftURL | safe }}">
              {{ hero.heroButtonLeftLabel | safe }}
              <span>
                {% include 'static/images/hero/button-container.njk' %}
              </span>
            </a>
          {% endif %}
          {% if hero.showHeroButtonRight == true %}
            <a href="{{ hero.heroButtonRightURL | safe }}">{{ hero.heroButtonRightLabel | safe  }}</a>
          {% endif %}
        </div>
      </section>
      {% endif%}
